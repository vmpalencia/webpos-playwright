// // tests/basic-ai.spec.ts
// import { test, expect } from '@playwright/test'
// import { ai } from '@zerostep/playwright'

// test('Navigate and verify content using ZeroStep', async ({ page }) => {
//   await page.goto('https://example.com')

//   await ai('Verify that the page heading contains the word Example', { page, test })
  
//   await expect(page.locator('h1')).toContainText('Example')
// })
